# 🔧 MyBatis-Flex 依赖问题 - 快速修复指南

## ❌ 错误信息
```
程序包com.mybatisflex.annotation不存在
```

## ✅ 解决方案（按顺序执行）

### 方法一：在 IntelliJ IDEA 中刷新 Maven（最简单）

1. **右键点击项目根目录** (`student-management-system`)
2. 选择 **`Maven`** → **`Reload Project`** 
3. 等待依赖下载完成（查看底部进度条）

**或者：**

1. 点击右侧边栏的 **`Maven`** 工具窗口图标
2. 点击刷新按钮 🔄 （Reload All Maven Projects）

---

### 方法二：使用命令行强制更新依赖

**Windows 用户：**

方式 1 - 直接运行批处理文件（推荐）：
```bash
双击运行：修复依赖.bat
```

方式 2 - 手动执行命令：
```cmd
cd e:\student-management-system\student-management-system
mvnw.cmd clean install -U
```

**Linux/Mac 用户：**
```bash
cd /path/to/student-management-system
./mvnw clean install -U
```

等待命令执行完成，看到 `BUILD SUCCESS` 即可。

---

### 方法三：在 IntelliJ IDEA 中重新构建

1. 菜单栏：**`Build`** → **`Rebuild Project`**
2. 等待构建完成

---

### 方法四：清除 IDE 缓存（如果上述方法都不行）

1. 菜单栏：**`File`** → **`Invalidate Caches / Restart...`**
2. 选择 **`Invalidate and Restart`**
3. IDE 会自动重启并重新索引项目

---

## 📋 推荐操作流程

### 第一次遇到此问题：
```
1. 右键项目 → Maven → Reload Project
   ↓ (等待完成)
2. Build → Rebuild Project
   ↓ (如果还有错误)
3. 运行：修复依赖.bat 或执行 mvnw.cmd clean install -U
```

### 如果还是不行：
```
4. File → Invalidate Caches / Restart... → Invalidate and Restart
```

---

## 🔍 验证修复成功

修复后应该：
- ✅ 红色错误消失
- ✅ 可以正常编译
- ✅ `Course.java`、`Student.java`、`Grade.java` 不再报错

---

## 💡 为什么会这样？

这个错误通常是因为：
1. **首次打开项目**：Maven 依赖还没有下载
2. **IDE 缓存问题**：IDE 没有识别到已下载的依赖
3. **网络问题**：依赖下载中断或不完整

---

## 📝 预防措施

以后打开项目时：
- ✅ 等待 Maven 依赖下载完成（查看右下角进度）
- ✅ 如果看到依赖下载失败的提示，点击重试
- ✅ 定期刷新 Maven 项目：`Maven` → `Reload Project`

---

## 🆘 还是不行？

检查以下几点：
1. ✅ Java 版本是否为 17+（`java -version`）
2. ✅ 网络连接是否正常
3. ✅ Maven 配置是否正确（`File` → `Settings` → `Build Tools` → `Maven`）
4. ✅ `pom.xml` 文件是否有语法错误

如果都正常但仍有问题，可以尝试删除本地 Maven 缓存后重新下载：
```
删除：C:\Users\你的用户名\.m2\repository\com\mybatisflex
然后重新执行：mvnw.cmd clean install -U
```
