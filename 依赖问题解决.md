# MyBatis-Flex 依赖问题解决方案

## 问题描述
编译错误：`程序包com.mybatisflex.annotation不存在`

## 解决方案

### 方法一：刷新 Maven 项目（IntelliJ IDEA）

1. **右键点击项目根目录** → `Maven` → `Reload Project`
   
   或
   
2. **点击右侧边栏的 Maven 图标** → 点击刷新按钮 🔄

3. **或者使用快捷键**：
   - 打开 Maven 工具窗口：`View` → `Tool Windows` → `Maven`
   - 点击刷新按钮（🔄 Reload All Maven Projects）

### 方法二：强制更新依赖

在项目根目录执行：

**Windows:**
```bash
mvnw.cmd clean install -U
```

**Linux/Mac:**
```bash
./mvnw clean install -U
```

`-U` 参数表示强制更新所有依赖。

### 方法三：清理并重新构建

**在 IntelliJ IDEA 中：**

1. 菜单栏：`Build` → `Rebuild Project`
   
2. 或者：`File` → `Invalidate Caches / Restart...` → 选择 `Invalidate and Restart`

**使用命令行：**
```bash
# Windows
mvnw.cmd clean compile

# Linux/Mac
./mvnw clean compile
```

### 方法四：添加 Maven 仓库（如果下载慢）

如果依赖下载很慢，可以在 `pom.xml` 中添加仓库配置。检查 `pom.xml` 中是否已有仓库配置，如果没有，可以添加：

```xml
<repositories>
    <repository>
        <id>central</id>
        <name>Maven Central</name>
        <url>https://repo1.maven.org/maven2</url>
    </repository>
    <repository>
        <id>aliyun</id>
        <name>Aliyun Maven</name>
        <url>https://maven.aliyun.com/repository/public</url>
    </repository>
</repositories>
```

### 方法五：检查依赖是否已下载

1. **查看本地 Maven 仓库**：
   - Windows: `C:\Users\你的用户名\.m2\repository\com\mybatisflex\`
   - Linux/Mac: `~/.m2/repository/com/mybatisflex/`

2. **如果目录不存在或文件不完整**，需要重新下载依赖。

### 方法六：检查网络和代理

如果是在公司网络或需要代理：

1. 配置 Maven 代理：编辑 `~/.m2/settings.xml`
2. 或在 IntelliJ IDEA 中：`File` → `Settings` → `Build, Execution, Deployment` → `Build Tools` → `Maven` → 配置代理

## 推荐操作步骤（按顺序尝试）

### 步骤 1：刷新 Maven 项目
```
右键项目 → Maven → Reload Project
```

### 步骤 2：如果步骤1不行，执行命令行强制更新
```bash
mvnw.cmd clean install -U
```
或
```bash
./mvnw clean install -U
```

### 步骤 3：在 IDE 中重新构建
```
Build → Rebuild Project
```

### 步骤 4：如果还是不行，清除 IDE 缓存
```
File → Invalidate Caches / Restart... → Invalidate and Restart
```

### 步骤 5：验证依赖已下载
检查 `pom.xml` 中的依赖是否正确，然后查看本地 Maven 仓库是否有文件。

## 验证修复

修复后，错误应该消失。如果还有问题，检查：

1. ✅ Java 版本是否为 17+
2. ✅ Maven 配置是否正确
3. ✅ 网络连接是否正常
4. ✅ IDE 的 Maven 设置是否正确

## 常见错误原因

1. **依赖未下载**：Maven 首次下载依赖需要时间
2. **IDE 缓存**：IDE 没有刷新 Maven 依赖
3. **网络问题**：无法访问 Maven Central 仓库
4. **版本冲突**：依赖版本不兼容（本项目使用的是 MyBatis-Flex 1.9.0）

## 如果所有方法都不行

可以尝试：
1. 删除 `.idea` 文件夹（如果使用 IntelliJ IDEA）
2. 删除本地 Maven 仓库中的 `com/mybatisflex` 文件夹，然后重新下载
3. 检查 `pom.xml` 是否有语法错误
4. 重启 IDE 和计算机

